SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Copy this file to .env.migration (gitignored) and fill in real values.
# Never put the service-role key in the app's own .env or prefix it EXPO_PUBLIC_ —
# it must never ship inside the mobile/web app bundle. See scripts/README-migration.md.
